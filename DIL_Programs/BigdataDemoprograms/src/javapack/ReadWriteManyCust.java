package javapack;

public class ReadWriteManyCust {
//copy paste from other pgrm not important
	/*
	 for(int j=0;j<lmt;j++)
		{
			
			for(int k=0;k<lmt-j;k++)
			{
				if(arr[k]>arr[k+1])
				{
					temp=arr[k];
					arr[k]=arr[k+1];
					arr[k+1]=temp;
				}System.out.println(arr[k]);
				
			}
		}
	 */
}
